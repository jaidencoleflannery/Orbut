import type { RenderContext } from './node';
import { Node } from './node';
export declare class Image extends Node {
    private sourceImage?;
    constructor(sourceImage?: HTMLImageElement | ImageBitmap | undefined);
    updateBitmap(newBitmap: ImageBitmap, bitmapPixelRatio: number, x: number, y: number): void;
    x: number;
    y: number;
    width: number;
    height: number;
    opacity: number;
    render(renderCtx: RenderContext): void;
}
